function norm(v){return String(v??'').toLowerCase().trim();}
function filterPhones(){const q=norm(document.getElementById('phoneSearch')?.value),b=document.getElementById('brandFilter')?.value||'';document.querySelectorAll('#phoneGrid .phone-card').forEach(c=>{const t=norm(c.dataset.name+' '+c.dataset.brand);c.style.display=(!q||t.includes(q))&&(!b||c.dataset.brand===b)?'':'none';});}
function filterPrices(){const q=norm(document.getElementById('priceSearch')?.value),b=document.getElementById('priceBrand')?.value||'';document.querySelectorAll('#priceGrid .price-card').forEach(c=>{const t=norm(c.dataset.name+' '+c.dataset.brand);c.style.display=(!q||t.includes(q))&&(!b||c.dataset.brand===b)?'':'none';});}
function sortPrices(){const g=document.getElementById('priceGrid');if(!g)return;const m=document.getElementById('priceSort')?.value||'default';const cards=[...g.querySelectorAll('.price-card')];cards.sort((a,b)=>{const av=a.dataset.price===''?Infinity:Number(a.dataset.price),bv=b.dataset.price===''?Infinity:Number(b.dataset.price);return m==='low'?av-bv:m==='high'?bv-av:0});cards.forEach(c=>g.appendChild(c));}
